# Redirect HTTP to HTTPS
server {
    listen 80;
    server_name kris-idp.org;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$server_name$request_uri;
    }
}

# HTTPS server
server {
    listen 443 ssl http2;
    server_name kris-idp.org;

    # SSL certificates
    ssl_certificate /etc/letsencrypt/live/kris-idp.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/kris-idp.org/privkey.pem;

    # SSL configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # Security headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options DENY always;
    add_header X-Content-Type-Options nosniff always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Frontend files
    root /usr/share/nginx/html;
    index index.html;

    # Backend API proxy (must be before catch-all)
    location /api {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Prometheus monitoring proxy
    location /prometheus {
        proxy_pass http://172.31.2.204/prometheus;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_buffering off;
    }

    # Grafana monitoring proxy
    location /grafana {
        proxy_pass http://172.31.2.204/grafana;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_buffering off;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Proxy ALL deployed microservices to k3s cluster
    # Matches services like: /kris-service-25/, /krisacc-svc-1/, /petstore-api-svc/
    # This uses regex to match common service patterns
    location ~ ^/([a-zA-Z0-9][a-zA-Z0-9-]*)(svc|service|api)([a-zA-Z0-9-]*)(/|$) {
        # Forward to k3s NGINX Ingress (private IP, port 80)
        proxy_pass http://172.31.2.204$request_uri;

        # Preserve original host header for ingress matching
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # Don't buffer large responses
        proxy_buffering off;
    }

    # Frontend routing (must come AFTER all proxies to avoid conflicts)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
