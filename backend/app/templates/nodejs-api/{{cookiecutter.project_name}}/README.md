# {{ cookiecutter.project_name }}

{{ cookiecutter.description }}

Generated by IDP Platform

## Local Development

```bash
# Install dependencies
npm install

# Run the application
npm start

# Or run with auto-reload
npm run dev
```

The application will be available at `http://localhost:{{ cookiecutter.port }}`

## Endpoints

- `GET /` - Root endpoint
- `GET /health` - Health check endpoint

## Running Tests

```bash
npm test
```

## Docker

Build and run with Docker:

```bash
docker build -t {{ cookiecutter.project_name }} .
docker run -p {{ cookiecutter.port }}:{{ cookiecutter.port }} {{ cookiecutter.project_name }}
```

## Deployment

This project is configured for automated deployment using:
- **GitHub Actions**: Builds and pushes Docker image on push to main
- **ArgoCD**: Automatically deploys to Kubernetes using Helm chart
- **Helm Chart**: Located in `helm/` directory

The CI/CD pipeline will:
1. Run tests
2. Build Docker image
3. Push to GitHub Container Registry
4. ArgoCD syncs and deploys to Kubernetes
