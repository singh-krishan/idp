global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'idp-backend'
    static_configs:
      - targets: ['backend:8000']
        labels:
          service: 'idp-backend'
          environment: 'development'
